#include "w3e26.h"

void testPrintArray (const uint8_t *byteArray)
{
  // std::cout << "================ \n";
  // for (size_t index = 0; index != 5000; ++index)
  // {
  //   if (index % 4 == 0)
  //     std::cout << "================ \n";
  //   std::cout << index << " : "
  //   << arrayIndex(index, 4)
  //   << " : "
  //   << withinIndex(index, 4)
  //   << " : "
  //   << getField(byteArray, index)
  //   << " : ";
  //   if (getField(byteArray, index) == 1)
  //     std::cout << "BUY";
  //   else if (getField(byteArray, index) == 2)
  //     std::cout << "SELL";
  //   else if (getField(byteArray, index) == 3)
  //     std::cout << "ELSE";
  //   else
  //     std::cout << "ELSE, NAMELY: \"" << (getField(byteArray, index) == 2) << "\"";
  //   std::cout << "\n";
  //
  // }
  // std::cout << "================ \n";
};
