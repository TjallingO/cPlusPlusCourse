#define ERR(msg) printf("%s : %d", (msg), __LINE__)

#include <thread>
#include <chrono>
#include <iostream>
#include <future>
#include <vector>
#include <algorithm>

using namespace std;

string threadFun(promise<bool> &prom);
