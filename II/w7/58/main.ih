#define ERR(msg) printf("%s : %d", (msg), __LINE__)

using namespace std;

#include "handler/handler.h"

#include <iostream>
#include <thread>
#include <fstream>

void execShift(Handler &myHandler, std::ostream &out, string const &text);
void defShift(ostream &out, string const &text);
