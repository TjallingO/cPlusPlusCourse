#include "parser.ih"

void Parser::functions(const std::string &text, RuleValue &input)
{
  // input.d_number = exp(valueOf(input));
  // input.d_type = RuleValue::VALUE;
  //
  // return input;
  cout << text << '\n';
}
