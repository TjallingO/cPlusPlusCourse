#define ERR(msg) printf("%s : %d", (msg), __LINE__)

using namespace std;

#include <set>
#include "flexcpp/Scanner.h"
#include <iostream>
#include <string>
#include <fstream>

bool file_exists(const char *fileName);
